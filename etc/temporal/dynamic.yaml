# Temporal dynamic config must be a map of dynamic keys to constrained values.
# Each key maps to a list of entries with `value` and optional `constraints`.

matching.longPollExpirationInterval:
  - value: "20s"
    constraints: {}
